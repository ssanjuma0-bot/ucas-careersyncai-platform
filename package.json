{
  "name": "ucas-careersync-ai-backend",
  "version": "1.0.0",
  "description": "UCAS CareerSync AI — real backend API with zero external dependencies (node:sqlite, crypto, http, net/tls, fetch).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js",
    "seed:admin": "node seed.js",
    "build": "echo \"No build step — plain Node.js, no bundler required.\"",
    "test": "node test-api.js"
  },
  "engines": { "node": ">=22.5.0" },
  "dependencies": {},
  "license": "UNLICENSED"
}
